# Story 47.6 Hybrid Narrow-Scope Execution Plan

Date: 2026-04-20
Owner: primary BMAD build agent
Story: `_bmad-output/implementation-artifacts/stories/epic-47/story-47.6.md`

## Scope Freeze (S0)

Locked decisions (user-approved recommended path):

1. Export format in-story: **CSV only** (PDF deferred follow-up).
2. Retention in-story: **policy + schema metadata only** (no archival worker in 47.6).
3. Auto snapshot trigger: on successful fiscal year close approve flow.
4. Implementation location: existing API purchasing stack (`apps/api/src/lib/purchasing/*`, `apps/api/src/routes/purchasing/reports/*`).

## Dependency-Aware Batching

### Batch A (sequential foundation)
- S1: migrations (snapshot + audit tables)
- S2: append-only immutability enforcement
- S3: shared/db contracts
- S4: snapshot core service (versioning + inputs hash + compare/export helpers)

### Batch B (parallel after S4)
- S5: API routes/ACL for snapshot endpoints
- S8: fiscal-close auto snapshot hook

### Batch C (parallel after Batch B)
- S6: compare response shaping hardening
- S7: CSV export endpoint behavior hardening

### Batch D (sequential closeout)
- S9: integration tests + regressions
- S10: adversarial review + docs/sprint status update

## Risk Gates

- P0 blocker: any mutability path for persisted snapshot balances.
- P1 blocker: cross-tenant leakage in list/get/compare/export.
- P1 blocker: version race creating duplicate/incorrect chain.
- P1 blocker: duplicate/incorrect auto snapshot behavior on approve replay.
