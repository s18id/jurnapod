{
  "$schema": "https://opencode.ai/config.json",

  // Optional default (you can override per run)
  "model": "openai/gpt-5.2-codex",
  // "model": "anthropic/claude-sonnet-4-5",
  // Agent definitions are sourced from .opencode/agents/*.md
  "default_agent": "codex-planner-agent",
  "instructions": [
    "AGENTS.md",
  ]
}
